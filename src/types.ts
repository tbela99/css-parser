/**
 * response type
 */
export enum ResponseType {

    /**
     * return text
     */
    Text,
    /**
     * return a readable stream
     */
    ReadableStream,
    /**
     * return an arraybuffer
     */
    ArrayBuffer
}